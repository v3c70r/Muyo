<!-- Generated by docs/gen_api_md.py from the Doxygen comments in src/RenderGraph/*.h. Do not edit by hand. -->

# RenderGraph API reference

Auto-generated from the Doxygen comments in `src/RenderGraph/*.h`. See [RenderGraph.md](RenderGraph.md) for the conceptual overview.

Coverage is enforced: `docs/check_api_coverage.py` fails the build when a public entity is undocumented.

## `DependencyGraph.h`

### `class Muyo::DependencyGraph`

A directed acyclic graph used to order render graph nodes.

| Member | Description |
| --- | --- |
| `void Muyo::DependencyGraph< T >::AddNode(const T &node)` | Register a node with no edges (in-degree 0) so it is included in the ordering. |
| `bool Muyo::DependencyGraph< T >::AddEdge(const T &from, const T &to)` | Add a dependency edge from -> to (i.e. to must run after from). |
| `std::vector< T > Muyo::DependencyGraph< T >::TopologicalSort() const` | Order all nodes so that every node appears after its dependencies (Kahn's algorithm). |
| `std::vector< std::vector< T > > Muyo::DependencyGraph< T >::GetParallelExecutionLevels() const` | Group nodes into levels that can run in parallel (Kahn's algorithm). One vector of nodes per dependency level, in order. |
| `bool Muyo::DependencyGraph< T >::IsAdjacentTo(const T &from, const T &to) const` | true if a direct edge from -> to exists. |
| `bool Muyo::DependencyGraph< T >::HasCycle() const` | true if the graph currently contains a cycle. |
| `size_t Muyo::DependencyGraph< T >::NodeCount() const` | Number of registered nodes. |


## `PSODesc.h`

### `enum BlendFactor`

Blend factor for color/alpha blending.

| Value | Description |
| --- | --- |
| `ONE` | 1.0. |
| `ZERO` | 0.0. |
| `SRC_ALPHA` | Source alpha. |
| `ONE_MINUS_SRC_ALPHA` | 1 - source alpha. |

### `enum BlendOp`

Blend operation.

| Value | Description |
| --- | --- |
| `ADD` | source + destination. |
| `SUBTRACT` | source - destination. |

### `struct Muyo::RenderGraph::BlendState`

Blend state for all color attachments of a node.

| Member | Description |
| --- | --- |
| `uint32_t attachmentCount` | Number of used attachments. |
| `std::array< ColorBlendAttachment, MAX_COLOR_ATTACHMENTS > attachments` | Per-attachment state. |

### `struct Muyo::RenderGraph::ColorBlendAttachment`

Per-attachment blend state.

| Member | Description |
| --- | --- |
| `bool blendEnable` | Enable blending for this attachment. |
| `BlendFactor srcColor` | Source color factor. |
| `BlendFactor dstColor` | Destination color factor. |
| `BlendOp colorOp` | Color blend operation. |
| `BlendFactor srcAlpha` | Source alpha factor. |
| `BlendFactor dstAlpha` | Destination alpha factor. |
| `BlendOp alphaOp` | Alpha blend operation. |
| `uint8_t colorWriteMask` | Which color channels are written. |

### `enum CompareOp`

Comparison operation used by depth/stencil state.

| Value | Description |
| --- | --- |
| `NEVER` | Never passes. |
| `LESS` | Passes if the new value is less. |
| `EQUAL` | Passes if equal. |
| `LESS_EQUAL` | Passes if less or equal. |
| `GREATER` | Passes if greater. |
| `NOT_EQUAL` | Passes if not equal. |
| `GREATER_EQUAL` | Passes if greater or equal. |
| `ALWAYS` | Always passes. |

### `VkPipeline Muyo::RenderGraph::CreatePipelineFromPSODesc(const PSODesc &psoDesc, VkDevice vkDevice, const std::vector< VkShaderModule > &shaderModuels, VkPipelineLayout pipelineLayout, VkPipelineRenderingCreateInfo renderingInfo)`

Build a graphics pipeline from a PSODesc using dynamic rendering.

### `enum CullMode`

Face culling mode.

| Value | Description |
| --- | --- |
| `NONE` | No culling. |
| `FRONT` | Cull front faces. |
| `BACK` | Cull back faces. |

### `struct Muyo::RenderGraph::DepthStencilState`

Depth and stencil state.

| Member | Description |
| --- | --- |
| `bool depthTestEnable` | Enable depth testing. |
| `bool depthWriteEnable` | Enable depth writes. |
| `CompareOp depthCompare` | Depth comparison operation. |
| `bool stencilEnable` | Enable stencil testing. |
| `StencilOpState front` | Front-face stencil state. |
| `StencilOpState back` | Back-face stencil state. |

### `enum FillMode`

Polygon fill mode.

| Value | Description |
| --- | --- |
| `SOLID` | Filled polygons. |
| `WIREFRAME` | Wireframe (VK_POLYGON_MODE_LINE). |

### `enum FrontFace`

Winding considered to be the front face.

| Value | Description |
| --- | --- |
| `CW` | Clockwise. |
| `CCW` | Counter-clockwise. |

### `struct Muyo::RenderGraph::InputAssemblyState`

Input assembly state (topology and primitive restart).

| Member | Description |
| --- | --- |
| `PrimitiveTopology topology` | Primitive topology. |
| `bool primitiveRestart` | Enable primitive restart. |

### `constexpr uint32_t MAX_COLOR_ATTACHMENTS`

Maximum number of color attachments a node can declare.

### `struct Muyo::RenderGraph::MultisampleState`

Multisample state.

| Member | Description |
| --- | --- |
| `uint8_t sampleCount` | Number of samples per pixel. |
| `bool sampleShadingEnable` | Enable sample shading. |
| `float minSampleShading` | Minimum fraction of sample shading. |

### `struct Muyo::RenderGraph::PSODesc`

Full pipeline state for a graphics node.

| Member | Description |
| --- | --- |
| `VertexType vertexType` | Vertex layout to use. |
| `InputAssemblyState inputAssembly` | Input assembly. |
| `RasterState rasterState` | Rasterization state. |
| `DepthStencilState depthStencilState` | Depth/stencil state. |
| `BlendState blendState` | Blend state. |
| `MultisampleState multisampleState` | Multisample state. |

### `enum PrimitiveTopology`

Primitive topology for a graphics pipeline.

| Value | Description |
| --- | --- |
| `TRIANGLES` | Triangle list. |
| `TRIANGLE_STRIP` | Triangle strip. |
| `LINES` | Line list. |
| `LINE_STRIP` | Line strip. |
| `POINTS` | Point list. |

### `struct Muyo::RenderGraph::RasterState`

Rasterization state.

| Member | Description |
| --- | --- |
| `FillMode fillMode` | Polygon fill mode. |
| `CullMode cullMode` | Face culling. |
| `FrontFace frontFace` | Front-face winding. |
| `bool depthClamp` | Enable depth clamping. |
| `bool depthBias` | Enable depth bias. |
| `int32_t depthBiasConstant` | Constant depth bias factor. |
| `float depthBiasSlope` | Slope depth bias factor. |

### `struct Muyo::RenderGraph::StencilOpState`

Stencil operations for one face.

| Member | Description |
| --- | --- |
| `uint8_t failOp` | Operation when the stencil test fails. |
| `uint8_t passOp` | Operation when the stencil test passes. |
| `uint8_t depthFailOp` | Operation when the stencil passes but depth fails. |
| `CompareOp compareOp` | Stencil comparison operation. |

### `constexpr VkBlendFactor Muyo::RenderGraph::ToVkBlendFactor(BlendFactor blendFactor)`

Maps BlendFactor to the Vulkan enum.

### `constexpr VkBlendOp Muyo::RenderGraph::ToVkBlendOp(BlendOp blendOp)`

Maps BlendOp to the Vulkan enum.

### `constexpr VkCompareOp Muyo::RenderGraph::ToVkCompareOp(CompareOp compareOp)`

Maps CompareOp to the Vulkan enum.

### `constexpr VkCullModeFlagBits Muyo::RenderGraph::ToVkCullMode(CullMode cullMode)`

Maps CullMode to the Vulkan enum.

### `constexpr VkFrontFace Muyo::RenderGraph::ToVkFrontFace(FrontFace frontFace)`

Maps FrontFace to the Vulkan enum.

### `constexpr VkPrimitiveTopology Muyo::RenderGraph::ToVkInputAssembly(PrimitiveTopology topology)`

Maps PrimitiveTopology to the Vulkan enum.

### `constexpr VkPolygonMode Muyo::RenderGraph::ToVkPolygonMode(FillMode fillMode)`

Maps FillMode to the Vulkan enum.

### `enum VertexType`

Which vertex layout a graphics node's pipeline expects.

| Value | Description |
| --- | --- |
| `GENERIC` | Vertex (position/normal/texcoord) used by scene geometry. |
| `UI` | ImGui's ImDrawVert used by the UI pass. |


## `RenderGraphBuilder.h`

### `constexpr int MAX_SHADER_STAGES`

Maximum number of shader stages a node may declare (vertex + fragment, or raygen/miss/hit...).

### `class Muyo::RenderGraph::RenderGraphBuilder`

Declares and runs a render graph.

| Member | Description |
| --- | --- |
| `RenderGraphBuilder(VkRenderDevice *renderDevice)` | renderDevice Device used to create pipelines, command buffers and synchronisation. |
| `~RenderGraphBuilder()` | Releases compiled pipelines, descriptor sets and command buffers. |
| `RenderGraphBuilder & AddResource(const ResourceHandle &handle, ResourceDesc desc)` | Declare a graph-owned resource. It is allocated at Build() from desc. *this for chaining. |
| `RenderGraphBuilder & ImportResource(const ResourceHandle &handle, const IRenderResource *resource)` | Register an externally owned resource (mesh buffers, scene data, TLAS...). Not allocated or freed by the graph. *this for chaining. |
| `std::optional< ResourceDesc > GetResourceDesc(const ResourceHandle &handle) const` | The allocation description for a graph-owned resource, if any. |
| `void AddNode(const RenderGraphNodeCreateInfo &nodeCreateInfo)` | Declare a node. Throws if the name is already used. |
| `void AddDependency(const std::string &fromNode, const std::string &toNode)` | Add an ordering edge: toNode runs after fromNode. Throws if either node is unknown or if the edge would create a cycle. |
| `void Build()` | Compile the graph: topological sort, resource allocation, pipeline/descriptor compilation and barrier planning. Call after all resources and nodes are declared. |
| `void Execute()` | Run every node once in execution order, inserting barriers between nodes and synchronising cross-queue handovers. |
| `std::vector< std::string > GetExecutionOrder() const` | The node names in dependency (topological) order. |

### `std::function< void(RenderGraphNodeContext &)> RenderGraphNodeCallback`

Callback a node provides to record its GPU or host work.

### `struct Muyo::RenderGraph::RenderGraphNodeContext`

Context handed to a node's execute callback.

| Member | Description |
| --- | --- |
| `QueueType queueType` | Queue this node is running on. |
| `RenderResourceManager & resourceManager` | Global resource manager (graph-owned resources). |
| `MeshResourceManager & meshManager` | Mesh manager (shared vertex/index buffers). |
| `VkCommandBuffer commandBuffer` | Command buffer to record into. |
| `VkPipeline pipeline` | Bound pipeline. |
| `VkPipelineLayout pipelineLayout` | Bound pipeline layout. |
| `VkPipelineBindPoint bindingPoint` | Bind point for the pipeline. |
| `T * GetResource(const ResourceHandle &handle) const` | Resolve a graph-declared resource to its concrete pointer (allocated at Build()). T Concrete resource type (e.g. BufferResource, RenderTarget). handle Resource name used in the node's resourceUses. The resource, or nullptr if it is not of type T. |

### `struct Muyo::RenderGraph::RenderGraphNodeCreateInfo`

User-facing declaration of a single render graph node (pass).

| Member | Description |
| --- | --- |
| `std::string nodeName` | Unique node name (used by AddDependency). |
| `QueueType queueType` | Queue the node runs on. |
| `bool async` | Opt-in: only when set (and queueType == COMPUTE) may the node be scheduled on the dedicated async compute queue and run concurrently with the graphics queue. Without it the node is recorded on the graphics queue, so no cross-queue synchronization is generated for it. |
| `std::vector< ResourceUse > resourceUses` | Resources the node reads/writes. |
| `std::vector< std::string > shaderNames` | Shader names for graphics (vert+frag) or compute. |
| `std::vector< std::string > rtShaderNames` | Ray tracing only: ray generation / miss / closest-hit shader names, in that order. When queueType == RAY_TRACING these are compiled into a ray tracing pipeline (with a graph-managed shader binding table) and the node automatically issues vkCmdTraceRaysKHR over the extent of its first STORAGE_IMAGE resource. Resources are bound by their explicit DescriptorBinding (reflection-derived set/binding), so a node can bind the TLAS, storage images and uniform buffers it declares. |
| `PSODesc psoDesc` | Graphics pipeline state (ignored for compute/RT). |
| `uint32_t costHint` | Reserved for the future scheduler. |
| `std::vector< VkClearValue > attachmentClearValues` | Optional clear values for the node's attachments, in attachment declaration order. |
| `RenderGraphNodeCallback execute` | Records the node's work. |

### `std::unordered_map< ResourceHandle, ResourceDesc > ResourceDescRegistry`

Map of every declared resource handle to its allocation description.


## `RenderGraphDescriptorSets.h`

### `struct Muyo::RenderGraph::BindingInfo`

Descriptor binding location (set + binding).

| Member | Description |
| --- | --- |
| `uint32_t bindingIndex` | Binding index within the set. |
| `uint32_t setIndex` | Descriptor set index. |

### `constexpr auto ENUM_COUNT`

Number of enumerators in an enum that has a trailing COUNT value.

### `struct Muyo::RenderGraph::EnumArray`

std::array indexed by an enum value.

| Member | Description |
| --- | --- |
| `std::array< T, ENUM_COUNT< E > > data` | Backing storage. |
| `constexpr T & Muyo::RenderGraph::EnumArray< E, T >::operator[](E e)` | Mutable reference to the element for enum value e. |
| `constexpr const T & Muyo::RenderGraph::EnumArray< E, T >::operator[](E e) const` | Const reference to the element for enum value e. |

### `constexpr size_t Muyo::RenderGraph::EnumIndex(E e)`

Converts an enum value to its underlying index.

### `const uint32_t MAX_BINDLESS_TEXTURE_COUNT`

Capacity of the bindless texture array in the MATERIAL set.

### `class Muyo::RenderGraph::RenderGraphDescriptorSets`

Owns the three built-in semantic descriptor set layouts used by graphics nodes.

| Member | Description |
| --- | --- |
| `RenderGraphDescriptorSets(DescriptorManager &descManager)` | Allocate the three semantic set layouts and their descriptor sets. descManager Descriptor manager owning the pool and layout cache. |
| `~RenderGraphDescriptorSets()` | Destroy the semantic set layouts owned by this instance. |
| `VkDescriptorSet AllocateSemanticSet(ResourceBindingSemantic bindingSemantic)` | The descriptor set for a semantic set index. Allocate a fresh descriptor set for one node from the shared layout of a semantic set. The caller owns the set and must free it with DescriptorManager::FreeDescriptorSet. |
| `VkDescriptorSetLayout GetDescriptorSetLayout(ResourceBindingSemantic bindingSemantic) const` | The descriptor set layout for a semantic set index. |
| `void BindResourceToDescriptorSet(VkDescriptorSet descriptorSet, VkDescriptorType descriptorType, const IRenderResource *pResource, uint32_t bindingIndex)` | Write a resource into a specific descriptor set. descriptorSet Target set (per node). descriptorType Type resolved via GetBindingType. pResource Buffer or image to bind. bindingIndex Binding within the set. |
| `static VkDescriptorType GetBindingType(ResourceBindingSemantic bindingSemantic, uint32_t bindingIndex)` | The descriptor type a semantic binding expects, or VK_DESCRIPTOR_TYPE_MAX_ENUM. |
| `static ResourceBindingSemantic GetSemantic(uint32_t setIndex)` | The semantic set corresponding to a raw descriptor set index (0/1/2). |

### `const std::vector< std::vector< VkDescriptorSetLayoutBinding > > bindingsPerSet`

Static descriptor bindings of the three built-in semantic sets.


## `RenderGraphNodeResource.h`

### `struct Muyo::RenderGraph::DescriptorBinding`

Explicit shader descriptor location (set + binding) for a resource.

| Member | Description |
| --- | --- |
| `uint32_t set` | Descriptor set index. |
| `uint32_t binding` | Binding index within the set. |

### `enum QueueType`

Which executor / queue a node runs on. CPU is treated as a queue inside the graph.

| Value | Description |
| --- | --- |
| `GRAPHICS` | Graphics queue. |
| `COMPUTE` | Compute queue (async only when the node opts in). |
| `COPY` | Transfer work (currently the graphics queue). |
| `RAY_TRACING` | Ray tracing pipeline, dispatched on the graphics queue. |
| `CPU` | Host-side node, executed inline while the graph runs. |
| `COUNT` | Number of queue types; not a valid value. |

### `struct Muyo::RenderGraph::ResolvedResourceUse`

A ResourceUse after the graph has resolved it to concrete barrier state.

| Member | Description |
| --- | --- |
| `ResourceHandle handle` | Name of the resource. |
| `ResourceVersion version` | Write version this use refers to. |
| `ResourceKind kind` | Concrete resource kind. |
| `ResourceIOType io` | Read / write / read-write. |
| `ResourceUsage usage` | Semantic role. |
| `VkPipelineStageFlags2 stages` | Pipeline stages that access the resource. |
| `VkAccessFlags2 access` | Access mask for the resource. |
| `VkImageLayout imageLayout` | Image layout (images only). |
| `VkFormat format` | Optional format (validation / pipeline creation). |
| `VkExtent3D extent` | Optional extent (validation / trace dispatch). |
| `ResourceBindingSemantic bindingSemantic` | Built-in set. |
| `std::optional< DescriptorBinding > descriptorBinding` | Explicit set/binding. |

### `enum ResourceBindingSemantic`

Built-in descriptor set the graph writes a resource into for graphics nodes.

| Value | Description |
| --- | --- |
| `PER_VIEW` | Set 0: per-frame view/camera data. |
| `PER_OBJ` | Set 1: per-object data. |
| `MATERIAL` | Set 2: materials and the bindless texture array. |
| `COUNT` | Number of semantic sets; not a valid value. |
| `NONE` | Not bound to a built-in semantic set. |

### `enum ResourceIOType`

Whether a node reads or writes a resource. Drives barrier generation.

| Value | Description |
| --- | --- |
| `READ` | Read-only access. |
| `WRITE` | Write-only access. |
| `READ_WRITE` | Read and write (e.g. an atomic counter). |
| `TRANSFER_WRITE` | Written by a transfer operation (e.g. vkCmdCopyBuffer). |

### `enum ResourceKind`

The concrete Vulkan object kind behind a resource handle.

| Value | Description |
| --- | --- |
| `IMAGE` | A VkImage (render target / storage image). |
| `BUFFER` | A VkBuffer. |
| `ACCELERATION_STRUCTURE` | A VkAccelerationStructureKHR (e.g. a TLAS). |
| `SAMPLER` | A VkSampler. |

### `enum ResourceUsage`

The semantic role of a resource within a node.

| Value | Description |
| --- | --- |
| `SAMPLED` | Sampled texture. |
| `STORAGE_IMAGE` | Read/write storage image. |
| `COLOR_ATTACHMENT` | Color render target. |
| `DEPTH_STENCIL_ATTACHMENT` | Depth/stencil render target. |
| `INPUT_ATTACHMENT` | Input attachment read. |
| `RESOLVE_ATTACHMENT` | Multisample resolve target. |
| `UNIFORM_BUFFER` | Uniform buffer. |
| `STORAGE_BUFFER` | Storage buffer. |
| `VERTEX_BUFFER` | Vertex buffer. |
| `INDEX_BUFFER` | Index buffer. |
| `INDIRECT_BUFFER` | Indirect draw/dispatch buffer. |
| `DRAW_COMMAND_BUFFER` | Draw/draw-count command buffer (written by compute, read by draws). |
| `ACCEL_STRUCTURE` | Top-level acceleration structure (TLAS). |
| `TASK_PAYLOAD` | Mesh shader task payload. |
| `MESH_PAYLOAD` | Mesh shader output payload. |
| `WORKGRAPH_NODE` | GPU work graph node record. |
| `PUSH_CONSTANTS` | Push constants (not resolved as a resource). |

### `struct Muyo::RenderGraph::ResourceUse`

A node's declaration that it uses a resource (the interface passed to RenderGraphBuilder).

| Member | Description |
| --- | --- |
| `ResourceHandle handle` | Name of the resource. |
| `ResourceIOType io` | Read / write / read-write. |
| `ResourceUsage usage` | Semantic role. |
| `ResourceKind kind` | Concrete resource kind. |
| `ResourceBindingSemantic bindingSemantic` | Built-in set for graphics nodes. |
| `std::optional< DescriptorBinding > descriptorBinding` | Optional explicit descriptor location. Only used when bindingSemantic == NONE. |

### `uint64_t ResourceVersion`

Monotonic version counter used to track resource writes across nodes.


## `RenderGraphResourceDesc.h`

### `IRenderResource * Muyo::RenderGraph::AllocateResource(const ResourceDesc &desc, RenderResourceManager &renderResourceManager, const ResourceHandle &handle)`

Allocate a physical resource from a desc. The render graph calls this at Build() time.

### `struct Muyo::RenderGraph::BufferResourceDesc`

Describes the allocation of a buffer. Size = count * stride.

| Member | Description |
| --- | --- |
| `uint64_t count` | Number of elements. |
| `uint64_t stride` | Size of one element in bytes. |
| `VkBufferUsageFlags usage` | Vulkan buffer usage flags. |
| `VmaMemoryUsage memoryProperties` | VMA memory usage. |
| `ResourceLifetime lifetime` | Lifetime relative to the graph. |

### `struct Muyo::RenderGraph::ImageResourceDesc`

Describes the allocation of an image (render target / storage image / texture).

| Member | Description |
| --- | --- |
| `VkFormat format` | Pixel format. |
| `VkExtent2D extent` | Width x height. |
| `uint32_t mips` | Mip level count. |
| `uint32_t layers` | Array layer count (6 = cubemap). |
| `VkImageUsageFlags usage` | Vulkan image usage flags. |
| `ResourceLifetime lifetime` | Lifetime relative to the graph. |

### `std::variant< BufferResourceDesc, ImageResourceDesc > ResourceDesc`

The complete set of resource types the graph knows how to allocate.

### `enum ResourceLifetime`

How long a resource lives relative to the graph.

| Value | Description |
| --- | --- |
| `Transient` | Lives only inside this graph; may be aliased / reused across nodes. |
| `Persistent` | Survives the frame boundary (e.g. TAA history, accumulation buffers). |
| `Imported` | Externally owned (old pass system, swapchain images); graph does not allocate. |


## `RenderGraphResourceHandle.h`

### `constexpr uint64_t Hash::StrFnV1A64(std::string_view str)`

FNV-1a 64-bit string hash. Reserved for a future hashed resource handle.

### `std::string ResourceHandle`

Stable string name identifying a resource, node or imported object in the graph.


## `ResourceUseResolver.h`

### `constexpr std::array< std::pair< UsageKey, UsagePolicy >, 26 > K_USAGE_POLICIES`

Barrier policy table: maps every supported (ResourceUsage, ResourceIOType) pair to the pipeline stages, access mask and image layout the graph uses when a node touches it.

### `constexpr UsagePolicy Muyo::RenderGraph::ResolvePolicy(UsageKey key)`

Looks up the UsagePolicy for a key; throws if the pair is unsupported.

### `ResolvedResourceUse Muyo::RenderGraph::ResolveResourceUse(const ResourceUse &use)`

Resolve a node's ResourceUse into a ResolvedResourceUse.

### `struct Muyo::RenderGraph::UsageKey`

Lookup key for the barrier policy table.

| Member | Description |
| --- | --- |
| `ResourceUsage usage` | Semantic usage. |
| `ResourceIOType io` | Read/write direction. |
| `constexpr bool operator==(const UsageKey &) const =default` | Equality for table lookup. |

### `struct Muyo::RenderGraph::UsagePolicy`

Barrier policy (stages/access/layout) for a (usage, io) pair.

| Member | Description |
| --- | --- |
| `VkPipelineStageFlags2 stages` | Destination pipeline stages. |
| `VkAccessFlags2 access` | Destination access mask. |
| `VkImageLayout layout` | Target image layout (ignored for buffers / AS / push constants). |
